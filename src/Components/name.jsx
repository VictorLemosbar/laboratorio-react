import React from "react";


function Nome(props) {
    return(
        <div>
            <p>{props.name}</p>
            <p>{props.idade}</p>
            <p>{props.Pais}</p>
        </div>

    )
};

export default Nome